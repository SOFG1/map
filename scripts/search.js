const config = {
  placeHolder: "Search for clubs",
  data: {
    src: ["Test", "ewfewwe", "eeeeeeeeeee"],
  },
  resultItem: {
    highlight: true,
  },
};

const autoCompleteJS = new autoComplete(config);
