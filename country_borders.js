// await waitForLoad();

// map.getStyle().layers.forEach((layer) => {
//   // Check for layers related to country labels
//   if (layer.id.includes("country-label")) {
//     map.setPaintProperty(layer.id, "text-color", "#FF5733"); // Change to your preferred color
//   }
// });
